<section class="coming-soon">
	<p class="brand">Finxiety</p>
	<h1>Coming soon.</h1>
	<p class="lede">Free financial tools, one step at a time. We&rsquo;re getting them ready.</p>
</section>

<style>
	.coming-soon {
		max-width: 32rem;
		margin: clamp(3rem, 12vh, 8rem) auto 0;
	}

	.brand {
		font-family: var(--font-display, inherit);
		font-weight: 600;
		letter-spacing: 0.02em;
		text-transform: lowercase;
		opacity: 0.7;
		margin-bottom: var(--space-sm, 0.75rem);
	}

	.coming-soon h1 {
		font-family: var(--font-display, inherit);
		font-weight: 600;
		font-size: clamp(2rem, 7vw, 2.75rem);
		line-height: 1.1;
		letter-spacing: -0.01em;
		margin-bottom: var(--space-sm, 0.75rem);
	}

	.lede {
		font-size: 1.125rem;
		line-height: 1.5;
		opacity: 0.8;
	}
</style>
