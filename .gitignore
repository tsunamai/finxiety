__pycache__/
*.pyc
.DS_Store

# Secrets — never commit
.env
.env.*
*.key
*.pem

# Personal data — never commit (threshold JSON tables, updated annually)
/finxiety/data/
# but DO track committed TS source data modules under the app's lib
!/finxiety/src/lib/data/

# Logs and local state
*.log
.claude/worktrees/

# Playwright MCP session snapshots — generated artifacts, not source
.playwright-mcp/

# SvelteKit / Node
node_modules/
.svelte-kit/
build/
dist/
.vercel/
